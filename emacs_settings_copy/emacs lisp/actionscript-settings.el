;; (add-to-list 'load-path (concat user-emacs-directory "emacs lisp/actionscript/"))

;; (require 'actionscript-mode)
;; ;;(require 'actionscript-config)




;; (defun set-as-mode ()
;;   (when (and (stringp buffer-file-name)
;;              (string-match ".as" buffer-file-name)
;;              (not (string-match ".asd" buffer-file-name)))
;;     (actionscript-mode)))

;; (add-hook 'find-file-hook 'set-as-mode)
